/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    // If you want to set a custom base path, e.g., for GitHub Pages repo name:
    // basePath: '/your-repo-name',
    // assetPrefix: '/your-repo-name/',
  };
  
  module.exports = nextConfig;